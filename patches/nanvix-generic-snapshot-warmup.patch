diff --git a/lib/nanvix/_boot.py b/lib/nanvix/_boot.py
index 31a4553..a28fb8b 100644
--- a/lib/nanvix/_boot.py
+++ b/lib/nanvix/_boot.py
@@ -35,6 +35,7 @@ def _do_warm_start():
     # Pre-warm the UTF-8 codec so the first encode/decode after restore
     # does not trigger lazy codec registry initialization.
     "".encode("utf-8")
     b"".decode("utf-8")
+    exec(compile("pass", "<snapshot-warmup>", "exec"), {"__name__": "__main__"})
-
+
     # Step 1: Take snapshot. On cold boot this saves state and continues.
